package com.example.icafe.features.dashboard.presentation

import androidx.lifecycle.ViewModel

class DashboardViewModel : ViewModel() {
    // This ViewModel can be expanded later to fetch dashboard data.
    // For now, it remains minimal as per the user's request, acting mostly as a navigation hub.
}